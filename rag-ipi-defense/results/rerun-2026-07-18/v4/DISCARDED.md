# Discarded run

Do not analyze or cite this directory. On Windows, an interrupted child process continued running while a resumed process wrote to the same checkpoint, so the output may contain racing writes. Both processes were terminated and the experiment was restarted from an empty directory.

Accepted replacement: `../v4-core-r2/`.
